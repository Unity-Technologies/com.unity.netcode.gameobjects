schema-version: 1.0.0
defaults:
  repo:
    choco:
      source-origin: https://artifactory-slo.bf.unity3d.com/artifactory/api/nuget/choco
packages:
  - name: utr
    origins:
      choco:
        package-version: 1.45.0
        package-id: standaloneutr
  - name: upm-pvp
    origins:
      choco:
        package-version: 0.78.0
        package-id: upm-pvp
