{% metadata_file .yamato/project.metafile %}
---
run_all_tests:
  name: Run All Package and Project Tests
  dependencies:
    # Pull in package and validate jobs through the badges job
    - .yamato/project-test.yml#badges_test_trigger
{% for editor in test_editors -%}
{% for platform in test_platforms -%}
{% for project in projects -%}
    - .yamato/package-tests.yml#test_{{ projects.first.packages.first.name }}_{{ editor }}_{{ platform.name }}
    - .yamato/project-test.yml#test_{{ project.name }}_{{ editor }}_{{ platform.name }}
{% endfor -%}
{% endfor -%}
{% endfor -%}

all_project_tests:
  name: Run All Project Tests
  dependencies:
    # Pull in package and validate jobs through the badges job
    #- .yamato/project-test.yml#badges_test_trigger
{% for editor in test_editors -%}
{% for platform in test_platforms -%}
{% for project in projects -%}
    - .yamato/project-test.yml#test_{{ projects.first.name }}_{{ editor }}_{{ platform.name }}
{% endfor -%}
{% endfor -%}
{% endfor -%}

all_package_tests:
  name: Run All Package Tests
  dependencies:
    # Pull in package and validate jobs through the badges job
    #- .yamato/project-test.yml#badges_test_trigger
{% for editor in test_editors -%}
{% for platform in test_platforms -%}
{% for project in projects -%}
    - .yamato/package-tests.yml#test_{{ projects.first.packages.first.name }}_{{ editor }}_{{ platform.name }}
{% endfor -%}
{% endfor -%}
{% endfor -%}